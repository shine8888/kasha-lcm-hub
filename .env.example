# Required for `npm run extract`. The Next.js app itself uses no secrets.
# Put your actual key in .env.local (gitignored), NOT this file.
ANTHROPIC_API_KEY=

# Override the extraction model if you want to compare runs.
# Defaults to claude-sonnet-4-6.
EXTRACTION_MODEL=
